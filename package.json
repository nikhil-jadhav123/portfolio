{
  "name": "nikhil-portfolio",
  "version": "1.0.0",
  "description": "Premium Data Engineer Portfolio with Glassmorphism Effects",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && uvicorn server:app --reload --host 0.0.0.0 --port 8001\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && uvicorn server:app --host 0.0.0.0 --port 8000",
    "install-all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "keywords": [
    "portfolio",
    "react",
    "fastapi",
    "mongodb",
    "data-engineer",
    "glassmorphism"
  ],
  "author": "Nikhil Jadhav",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}